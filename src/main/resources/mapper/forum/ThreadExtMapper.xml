<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.xdea.xmux.forum.mapper.ThreadExtMapper">
    <resultMap id="BaseResultMap" type="io.xdea.xmux.forum.model.Thread">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Mar 07 13:48:24 CST 2022.
        -->
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="uid" jdbcType="VARCHAR" property="uid" />
        <result column="forum_id" jdbcType="INTEGER" property="forumId" />
        <result column="title" jdbcType="VARCHAR" property="title" />
        <result column="body" jdbcType="VARCHAR" property="body" />
        <result column="create_at" jdbcType="TIMESTAMP" property="createAt" />
        <result column="update_at" jdbcType="TIMESTAMP" property="updateAt" />
        <result column="like" jdbcType="INTEGER" property="like" />
        <result column="pinned" jdbcType="BIT" property="pinned" />
        <result column="digest" jdbcType="BIT" property="digest" />
    </resultMap>

    <select id="selectSaved" resultMap="BaseResultMap">
        select t.*
        from forum.thread t right join forum.saved_thread s on t.id = s.thread_id
        where s.uid = #{uid}
        order by s.create_at desc
        limit #{limit} offset #{offset}
    </select>

    <update id="setUpdateTimeToNow">
        update forum.thread
        set update_at = now()
        where id = #{id}
    </update>

    <update id="incVote">
        update forum.thread
        set "like" = "like" + 1
        where id = #{id}
    </update>

    <update id="decVote">
        update forum.thread
        set "like" = "like" - 1
        where id = #{id}
    </update>

    <update id="toggleBest">
        update forum.thread
        set digest = not digest
        where id = #{id}
    </update>

    <update id="toggleTop">
        update forum.thread
        set pinned = not pinned
        where id = #{id}
    </update>
</mapper>