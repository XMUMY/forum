<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.xdea.xmux.forum.mapper.PostExtMapper">
    <resultMap id="BaseResultMap" type="io.xdea.xmux.forum.model.Post">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Dec 30 18:47:49 CST 2021.
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="uid" jdbcType="VARCHAR" property="uid"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="body" jdbcType="VARCHAR" property="body"/>
        <result column="vote" jdbcType="INTEGER" property="vote"/>
        <result column="group_id" jdbcType="INTEGER" property="groupId"/>
        <result column="topped" jdbcType="BIT" property="topped"/>
        <result column="best" jdbcType="BIT" property="best"/>
        <result column="delete_time" jdbcType="TIMESTAMP" property="deleteTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <resultMap id="PostWithGroupName" type="io.xdea.xmux.forum.model.PostWithGroupName" extends="BaseResultMap">
        <result column="group_name" jdbcType="VARCHAR" property="groupName"/>
    </resultMap>

    <select id="selectWithLimitOffset" resultMap="PostWithGroupName">
        select p.id, uid, p.create_time, update_time, p.delete_time, p.title, body, vote, group_id, topped,
        best, g.title group_name from forum.post p left join forum.group g on
        p.group_id = g.id
        where p.delete_time is null
        <if test="cId != null">
            and group_id in
            <foreach collection="cId" item="acId" separator="," open="(" close=")">
                #{acId}
            </foreach>
        </if>
        order by topped desc, update_time desc limit #{limit} offset #{offset}
    </select>

    <update id="setDeleteTimeToNow">
        update forum.post
        set delete_time = now()
        where id = #{id}
    </update>

    <update id="setUpdateTimeToNow">
        update forum.post
        set update_time = now()
        where id = #{id}
    </update>

    <update id="incVote">
        update forum.post
        set vote = vote + 1
        where id = #{id}
    </update>

    <update id="decVote">
        update forum.post
        set vote = vote - 1
        where id = #{id}
    </update>

    <update id="toggleBest">
        update forum.post
        set best = not best
        where id = #{id}
    </update>

    <update id="toggleTop">
        update forum.post
        set topped = not topped
        where id = #{id}
    </update>
</mapper>